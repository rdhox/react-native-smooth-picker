describe("interaction", () => {
  it("It should trigger onSelected props function when selected", () => {
    const fnMock = jest.fn();
  });
  // it("It should have an magnet props that influence the selection");
  // it("It should select value where offset is decided by user");
});

describe("operation", () => {
  it(
    "It should not test every item at each scroll event, only few item around the one selected"
  );
});

// describe("render", () => {
//   it("It should display the default selected value as selected after mounted");
//   it("It should change the style of selected element when selected");
// });
